"""Implements trie class. Loads dictionary into memory via trie data structure,
navigates through trie to check spelling, uses trie to suggest similar words.
Writes to output file."""


class Trie(object):
    def __init__(self):
        # instance variables
        i = 0

    # Returns true if trie is successfully loaded into memory. Otherwise, return true
    def status(self):
        return False
